---
description: Python Flask backend coding standards for this project
globs: backend/**/*.py
alwaysApply: false
---

### 后端代码规范（Python + Flask）

- **整体风格**
  - 使用 **PEP 8** 代码风格，统一用 **4 个空格缩进**。
  - 文件编码使用 UTF-8，文件末尾保留一个空行。
  - 模块内 import 顺序：标准库 -> 第三方库 -> 本项目内部模块，并用空行分隔。

- **项目结构**
  - 按功能拆分模块，避免单文件过大（推荐 < 500 行）。
  - 视图/路由、业务逻辑、数据访问层、配置应分离，避免所有逻辑都写在视图函数中。
  - 公共工具函数放在 `utils` 或 `common` 模块中，避免在多个文件中复制粘贴。

- **命名规范**
  - 模块、文件名：`snake_case`，例如 `user_service.py`。
  - 函数、变量：`snake_case`，例如 `get_user_books`。
  - 类名：`PascalCase`，例如 `UserService`。
  - 常量：`UPPER_SNAKE_CASE`，例如 `DEFAULT_PAGE_SIZE`。
  - Flask Blueprint 建议使用业务语义命名，例如 `user_bp`、`auth_bp`。

- **类型注解**
  - 新增或修改的函数，优先增加 **类型注解**（参数和返回值）。
  - 对于返回 dict / list 的接口，尽量使用 `TypedDict`、`dataclasses` 或 Pydantic 模型（如果有）来提升可读性。

- **配置与敏感信息**
  - 所有配置通过 `config.py` 或专门的配置模块/类管理，不在业务代码中硬编码。
  - 密钥、数据库密码等敏感信息必须通过环境变量或配置文件读取，不写入仓库。

- **Flask 视图与路由**
  - 每个接口的路由路径使用 **短而语义清晰的 RESTful 风格**，如 `/api/users/<int:user_id>/books`。
  - 统一使用 `Blueprint` 管理路由，避免所有路由集中在单一文件。
  - 视图函数只负责：
    - 解析请求参数
    - 调用业务服务层
    - 统一封装响应结果
  - 不在视图函数中直接操作数据库，数据库操作放在 DAO / Repository 层或 Service 层。

- **请求参数与验证**
  - 所有外部输入都必须进行验证（类型、必填、取值范围），避免直接信任前端。
  - 对复杂请求体，优先使用 schema（如 Pydantic / Marshmallow），否则自行编写校验逻辑，并集中封装。
  - 对分页、排序等通用参数封装统一解析函数，避免在每个接口中重复解析。

- **返回值与响应格式**
  - 统一使用标准 JSON 响应结构，例如：
    - `{"code": 0, "msg": "ok", "data": {...}}`
    - 出错时：`{"code": 错误码, "msg": "错误信息", "data": null}`
  - HTTP 状态码与业务 code 要区分使用：
    - HTTP 2xx 表示请求技术层面成功。
    - 业务错误通过业务 code 表达，如 40001（参数错误）、40003（未登录/无权限）等。

- **错误处理**
  - 不允许出现空的 `except` 或捕获异常但不处理的情况：
    - 必须至少记录日志，并根据业务需要转换为统一的业务异常或响应。
  - 常见异常（如认证失败、权限不足、资源不存在）应定义为自定义异常类，并通过 Flask 统一错误处理（`errorhandler` 或全局异常钩子）转换为标准响应。
  - 不在多处复制异常处理逻辑，尽量集中在统一的异常处理模块。

- **日志规范**
  - 使用 Python 标准日志库或项目统一封装的 logger，不使用随意的 `print`。
  - 重要操作必须记录结构化日志（包含用户 ID、请求路径、重要参数、错误堆栈等）。
  - 日志级别约定：
    - `debug`: 调试信息
    - `info`: 关键业务流程记录（如登录成功、下单成功）
    - `warning`: 非预期但可容忍情况（如参数缺失使用默认值）
    - `error`: 业务错误或第三方服务调用失败
    - `critical`: 导致服务不可用的严重错误

- **数据库与事务**
  - 所有数据库访问应通过统一的会话管理（如 `scoped_session` 或封装好的 `db.session`）。
  - 涉及多表写入的操作应在事务中完成，出错时确保回滚。
  - 严禁在循环中频繁开启/提交事务，尽量批量操作。

- **性能与安全**
  - 对可能造成 N+1 查询的问题进行优化（如使用预加载、批量查询）。
  - 所有外部输入都要防止 SQL 注入、XSS 等常见安全问题（ORM 默认可避免大部分 SQL 注入，但仍要避免字符串拼 SQL）。
  - 对敏感接口增加鉴权与权限控制，避免只在前端控制可见性。

- **测试与可维护性**
  - 重要的服务逻辑和工具函数应具备单元测试或集成测试。
  - 函数尽量保持单一职责，避免巨型函数。
  - 当某个文件/函数变得难以理解或修改时，优先进行重构而不是继续堆叠逻辑。

