---
description: Python Flask backend coding standards for this project
globs: backend/**/*.py
alwaysApply: false
---

### 后端代码规范（Python + Flask，贴合当前项目日志体系）

- **整体风格**
  - 使用 **PEP 8**，统一 **4 空格缩进**，UTF-8 编码，文件末尾保留一个空行。
  - import 顺序：标准库 -> 第三方 -> 本项目模块，并用空行分隔。
  - 新增/修改函数优先补齐类型注解（参数 + 返回值）。

- **项目结构**
  - 路由与业务逻辑分离：视图函数负责参数解析、鉴权、响应组装；核心逻辑放 Service 层。
  - 数据访问统一走 SQLAlchemy 会话（`db.session`），不要在视图层散落 SQL 细节。
  - 复用逻辑抽到公共模块，避免重复实现（尤其是参数校验、日志埋点、第三方调用封装）。

- **命名规范**
  - 文件/模块/函数/变量使用 `snake_case`，类名使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。
  - Blueprint 命名与业务域一致（如 `auth_bp`、`user_bp`、`creator_bp`、`admin_bp`）。
  - 日志 `event` 使用稳定语义前缀：`business.*`、`dependency.*`、`http.*`、`app.*`。

- **日志规范（本项目重点）**
  - **必须使用结构化 JSON 日志**，统一走 `app.logger` 与 `app.logging_utils`，禁止 `print`。
  - 日志字段遵循现有格式：`timestamp`、`level`、`message`、`event`、`tags`、`data`。
  - 请求链路字段由框架钩子注入：`request.request_id`、`method`、`path`、`query_string`、`remote_addr`、`user_agent`。
  - 身份字段使用 `identity`：`user_id`、`username`、`role`、`is_admin`、`auth_state`。
  - 业务日志优先用 `log_business_event(...)`；第三方/存储调用优先用 `log_dependency_call(...)`。
  - 对可复用流程优先使用 AOP 装饰器：`business_log_aspect(...)`、`dependency_log_aspect(...)`。
  - 依赖调用日志必须记录：`dependency.name`、`dependency.operation`、`data.success`、`data.elapsed_ms`。
  - 异常日志必须包含异常堆栈（`exc_info=True` 或 `logger.exception(...)`），避免只有文本无上下文。
  - `tags` 建议保持可检索性：按 `业务域 + 行为 + 类型` 组合（如 `["dependency","cos","upload"]`）。

- **日志级别约定（结合现状）**
  - `INFO`：正常业务事件与访问日志（如登录、鉴权拒绝、请求完成）。
  - `ERROR`：依赖调用失败、未处理异常、影响请求结果的错误。
  - `WARNING`：兼容性降级、可恢复问题（如补丁跳过、回退路径触发）。
  - 不滥用 `ERROR`；可预期业务分支（例如未登录返回 401）默认记录为 `INFO` 业务事件。

- **请求与错误处理**
  - 统一使用 `before_request`/`after_request`/`teardown_request` 保证链路日志完整性，不绕过请求钩子。
  - 所有异常要么被明确处理并记录结构化日志，要么交给统一错误处理器。
  - 发生 5xx 时响应应包含 `request_id`，便于按日志反查问题。
  - 禁止空 `except`；捕获异常后至少记录日志并返回可诊断信息。

- **参数验证与安全**
  - 外部输入（query/body/path/header）必须做类型与范围校验，不信任前端。
  - JWT/鉴权相关代码要明确设置并维护 `auth_state`（如 `anonymous`/`authenticated`/`token_verified`/`invalid_token`）。
  - 禁止硬编码密钥、COS 凭据、数据库密码；全部走配置/环境变量。

- **数据库与事务**
  - 多表写入必须放在事务中，失败要回滚；避免循环中频繁提交。
  - 新增查询注意 N+1 风险，优先批量或预加载方案。
  - 兼容性 SQL（如 schema patch）应带日志，便于审计和排障。

- **可维护性与测试**
  - 关键业务函数、依赖适配层（如 COS、本地存储）应有单元测试或最小集成测试。
  - 增加新接口时至少验证：成功路径、鉴权失败路径、依赖失败路径是否都有可检索日志。
  - 当函数复杂度持续上升时优先重构，保持单一职责。

