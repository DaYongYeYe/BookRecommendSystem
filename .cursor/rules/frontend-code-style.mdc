---
description: Vue 3 + TypeScript frontend coding standards for this project
globs: frontend/src/**/*.{ts,tsx,js,jsx,vue}
alwaysApply: false
---

### 前端代码规范（Vue 3 + TypeScript）

- **整体风格**
  - 使用 **TypeScript** 编写业务逻辑，统一开启严格类型检查（以项目 tsconfig 为准）。
  - 统一使用 **单引号** 或 **双引号**，与现有项目保持一致，避免混用。
  - 禁止使用 `any` 作为兜底类型，除非有明确理由，并在必要处添加注释。
  - 统一使用 ES 模块 `import/export`，避免使用 `require`。

- **项目结构**
  - 视图组件放在 `views`，可复用通用组件放在 `components`。
  - API 请求封装在 `api` 目录中，前端不得直接写死后端 URL。
  - 公共工具函数放在 `utils` 或 `hooks`（如 `useXxx`）目录中，避免多处复制逻辑。
  - 路由、状态管理、全局配置（如 `main.ts`, `router`, `store`）保持简单清晰，复杂逻辑下沉到业务模块。

- **命名规范**
  - 组件文件名：`PascalCase.vue`，例如 `RegisterForm.vue`。
  - 组件名（`name` 或 `<script setup>` 默认名）：使用 `PascalCase`，与文件名一致。
  - 变量、函数、普通常量：`camelCase`，例如 `getUserBooks`。
  - 全局常量或配置：`UPPER_SNAKE_CASE`，例如 `BASE_API_URL`。
  - 组合式函数：以 `use` 为前缀，如 `useAuth`, `useBookList`。

- **Vue 组件书写规范**
  - 默认使用 **Composition API**（`<script setup>` 或 `setup()`），避免新代码继续使用 Options API，除非与旧代码兼容有明确需求。
  - 模板中：
    - 指令顺序建议：`v-if` / `v-for` / `v-show` -> 绑定类/样式 -> 事件。
    - `v-for` 必须提供稳定的 `:key`。
    - 避免在模板中编写复杂表达式，将其抽到计算属性或方法中。
  - `<script>` 中：
    - 引入顺序：Vue/框架核心 -> 第三方库 -> 本项目模块 -> 样式。
    - 组合式 API（`ref`, `computed`, `watch`, `onMounted` 等）使用解构导入。

- **状态管理与数据流**
  - 表单和局部 UI 状态尽量保持在本组件或最近的父组件中。
  - 跨页面/全局状态通过状态管理（如 Pinia / Vuex）或顶层组件提供的 context 管理，不滥用全局变量。
  - 避免在多个组件中重复请求同一份数据，可封装为 `useXxx` 组合式函数复用。

- **API 请求与错误处理**
  - 所有网络请求通过统一的请求封装（如 `request.ts`），不直接在组件中使用 `fetch` / `axios` 等。
  - 封装层负责：
    - 附带 Token / 通用头信息
    - 统一处理基础错误（如网络错误、超时、未登录跳转）
    - 解析后端返回的统一结构（如 `code`、`msg`、`data`）
  - 组件内只处理与当前场景相关的错误提示（如表单校验错误、业务失败提示），通过统一的消息组件（如全局 Message/Notification）。

- **表单与校验**
  - 所有表单提交前必须进行前端校验，避免无效请求进入后端。
  - 对密码、邮箱等敏感字段进行基本格式校验，并在界面上给出明确反馈。
  - 表单模型使用类型定义，避免 `any` 或隐式 `any`。

- **样式与布局**
  - 样式优先使用组件内 scoped 样式或 CSS Modules，避免全局样式污染。
  - 公共主题变量（颜色、间距、字号）集中管理（如 `variables.scss` 或 CSS 变量）。
  - 禁止随意写行内样式，除非是临时/动态且难以抽离的场景。
  - 响应式布局遵循移动优先或桌面优先的一致策略，避免在同一项目中风格混乱。

- **Tailwind CSS（本项目为 v4）**
  - 入口 CSS 必须使用 v4 语法：`@import "tailwindcss";`。禁止使用 v3 的 `@tailwind base;` / `@tailwind components;` / `@tailwind utilities;`，否则样式不会生效。
  - 检测 Tailwind 是否生效：
    1. **开发者工具**：在元素上右键「检查」，看 Styles 面板中是否存在 `.w-10`、`.h-10`、`.rounded-full` 等类及其对应样式（如 `width`、`height`、`border-radius`）。
    2. **快速自测**：临时给某元素加 `class="text-red-600 text-3xl font-bold"`，若页面出现红色大号加粗文字则生效，测完删除。
    3. **Network**：DevTools → Network → 筛选 CSS，刷新后打开主样式文件，搜索 `w-10` 或 `rounded-full`，能搜到即说明 Tailwind 已参与构建。
  - 修改 Tailwind 入口或配置后需重启开发服务器（`npm run dev`）再验证。

- **路由与导航**
  - 路由路径语义化，避免无意义路径名，如 `/a`, `/page1`。
  - 路由配置与组件解耦，避免在组件中硬编码大量路由字符串，尽量集中管理常用路由常量。
  - 对需要登录的路由，通过路由守卫统一处理鉴权逻辑。

- **可读性与可维护性**
  - 组件单文件尽量控制在合理大小（推荐 < 300 行），超出时考虑拆分子组件或抽取逻辑。
  - 不在同一组件中混合太多职责（如网络请求、复杂表单逻辑、大量 UI 逻辑），适当拆分。
  - 对于复杂逻辑（如推荐算法参数处理、复杂交互）添加简明注释，解释「为什么这样做」而不是「代码在做什么」。

- **调试与日志**
  - 禁止在提交代码中保留 `console.log`、`debugger`，调试用完必须删除。
  - 如果需要在生产环境中记录前端行为或错误日志，统一通过封装的日志/埋点工具上报。

- **性能与体验**
  - 对列表渲染、大量 DOM 节点操作优先考虑虚拟列表、分页、懒加载等优化方案。
  - 减少不必要的重新渲染，合理使用计算属性、`watch`、`watchEffect`，避免滥用深度监听。
  - 图片、静态资源合理使用压缩与缓存策略。

- **代码提交相关**
  - 提交前运行格式化（如 Prettier、ESLint）和构建检查，确保无明显语法/类型错误。
  - 提交信息应简洁清晰，说明变更意图（如 `feat: 注册页增加密码强度校验`）。

